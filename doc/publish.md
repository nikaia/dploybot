# Publish
https://docs.npmjs.com/getting-started/publishing-npm-packages

1. Update master and push
2. Login to npm using `npm login` (account `nikaia`)
3. update version (or use `npm version patch`)
4. npm publish 